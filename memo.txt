				c		c++
헤더파일	stdio.h		iostream
입력문		scanf		cin
출력문		pirntf		cout

std:: :소속을 알리는 네임스페이스 

네임스페이스 : 내부식별자에 범위를 지정하는 선언적 영역으로 여러 라이브러리를 포함할 때 발생할 수 있는 이름 충돌을 방지하기 위해 사용 

std는 standard의 줄임말로 함수, 클래스, 객체 등이 표준 라이브러리에 소속되어 있음을 나타냅니다.

c언어와 달리 cout는 형식지정자 사용 안하고 정수, 문자형, 부동소수 출력 가능 
<< 를 여러개 사용해서 연속해서 출력도 가능 
 ex) cout << "Hello" << 123 << 3.14; 

c언어와 달리 cin은 형식지정자 사용 안하고 입력 가능, 그리고 데이터 형식에 scanf보다 안전  

void 사용 상황
1. 함수가 값을 반환하지 않음을 표시할 때 
2. 함수의 매개변수가 없을 때	
3. 어떤 변수라도 가리킬 수 있는 제너릭 포인터를 만들 때 (형태가 자유로움을 의미)

							char					wchar_t
인코딩 방식			멀티바이트					유니코드
단일 문자 크기		1byte 또는 2byte			2byte
문자열				유니코드를 제외한 문자열	와이드문자, UTF-16으로 인코딩된 문자열

wchar_t는 wide character type의 약자로, 멀티바이트 문자셋을 지원하는 C++에서 유니코드 문자를 표현하기 위해 사용
wchar_t로 출력시 wcout 사용, 입력시 wcin 사용

cout는 부동소수점 출력시 기본 정밀도 6으로 설정되어 소수점 6자리까지만 출력

부동소수점 표현시
뒤에 f -> float형
뒤에 l -> long double형
뒤에 아무것도 없으면 -> double형

setprecision(numeric_limits<데이터형식>::digits10 + 1); // 특정 데이터 형식이 표현될 수 있는 유효자릿수만큼 정밀도 저정 

구문범위 : if, while 등의 구문 안에서만 유효
지역 범위 : 지역에서만 유효
전역 범위 : 프로그램 전체에서 유효
클래스 범위 : 클래스 안에서만 유효
네임스페이스 범위 : 네임스페이스 안에서만 유효